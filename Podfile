# source 'https://github.com/CocoaPods/Specs.git'

platform :ios, "8.0"
use_frameworks!

target "ParkenDD" do
  pod 'ParkKit', '~> 0.2'
  pod 'SwiftyTimer', '~> 2.0'
  pod 'ObjectMapper', '~> 2.2'
  pod 'SwiftyDrop', '~> 3.0'
  pod 'Charts', '~> 3.0'
end
